// API request/response shapes — used by route handlers and Server Actions.
//
// The ask endpoint's contract lands here in Milestone 1. It is deliberately
// non-streaming: an answer is only ever returned after its citations have been
// verified against the retrieved context. See ADR-005.

export {};
