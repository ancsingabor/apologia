-- Seed data for local development.
-- Add your own admin email to the allowlist so you can sign in after running
-- the migrations. Replace the address below, then re-run `supabase db reset`.

-- insert into admin_users (email, role)
-- values ('you@example.com', 'admin')
-- on conflict (email) do nothing;
